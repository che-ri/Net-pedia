import React from 'react';
import Layout from '../components/Layout';

const Test = () => {
    return (
        <Layout>
            <span>TEST 중!</span>
        </Layout>
    );
};

export default Test;
